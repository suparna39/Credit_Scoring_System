import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ['svc-c66b3991-morphvm-cq04yeet.http.cloud.morph.so'],/* config options here */
};

export default nextConfig;
